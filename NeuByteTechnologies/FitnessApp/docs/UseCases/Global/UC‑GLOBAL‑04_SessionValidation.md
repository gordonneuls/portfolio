# UC‑GLOBAL‑04 — Session Validation
## Purpose: 
Validate session token on every protected page load.
## Includes:
-	Redirect to Login if token missing/expired
-	Refresh session metadata
-	Enforce access control